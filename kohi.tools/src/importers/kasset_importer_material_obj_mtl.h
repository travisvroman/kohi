#pragma once

#include "defines.h"
#include "strings/kname.h"

KAPI b8 kasset_material_obj_mtl_import(const char* output_directory, const char* output_filename, kname package_name, const char* data);
